% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spToolsOutput.R
\name{sp.save}
\alias{sp.save}
\title{Save Output to File}
\usage{
sp.save(
  x,
  file = NULL,
  dir = ".",
  format = c("rds", "txt", "csv"),
  name = NULL,
  digits = 3,
  row.names = FALSE,
  ...
)
}
\arguments{
\item{x}{The object to save (e.g., a list, data frame, or matrix).}

\item{file}{Optional file name (including extension: \code{.rds}, \code{.txt}, or \code{.csv}).}

\item{dir}{Output directory. If it does not exist, it will be created. Default is \code{"."}.}

\item{format}{File format: \code{"rds"} (default), \code{"txt"}, or \code{"csv"}.}

\item{name}{Optional name for auto-naming when \code{file} is \code{NULL}.}

\item{digits}{Number of digits to round numeric values for text or CSV output.}

\item{row.names}{Logical; whether to include row names in CSV/TXT. Default is FALSE.}

\item{...}{Additional arguments passed to \code{write.table()} or \code{write.csv()}.}
}
\value{
Invisibly returns the full path to the saved file.
}
\description{
Saves a result object to an \code{.rds}, \code{.txt}, or \code{.csv} file. If \code{file} is not provided,
a name is automatically generated using the object's name or timestamp.
}
\examples{
# Example object to save
result <- data.frame(Estimate = 0.52, SE = 0.12, df = 24)

# Save as RDS with automatic filename
preserve(result)

# Save as TXT with a custom filename
preserve(result, file = "ci_output.txt", format = "txt")

# Save as CSV with an auto-generated name, into a subfolder
preserve(result, format = "csv", dir = "outputs", name = "ci_mean1")

# Save a list of results as RDS
res_list <- list(t1 = result, t2 = result)
sp.save(res_list, file = "results_list.rds")

}
