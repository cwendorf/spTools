% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spToolsWrappers.R
\name{ci.lc.mean.ws.complex}
\alias{ci.lc.mean.ws.complex}
\title{Confidence Intervals for Two Within-Subjects Mean Contrasts and Their Difference}
\usage{
ci.lc.mean.ws.complex(alpha, m, s, R, n, q1, q2, labels = NULL)
}
\arguments{
\item{alpha}{Numeric scalar. Significance level (e.g., 0.05 for 95\% CI).}

\item{m}{Numeric vector of length J. Means for each condition.}

\item{s}{Numeric vector of length J. Standard deviations for each condition.}

\item{R}{Either a scalar (average correlation among all condition pairs) or a J x J
correlation matrix among conditions.}

\item{n}{Integer scalar. Sample size (number of subjects).}

\item{q1}{Numeric vector of length J. Contrast weights for the first contrast.}

\item{q2}{Numeric vector of length J. Contrast weights for the second contrast.}

\item{labels}{Character vector of length 2. Optional row labels for the two contrasts.}
}
\value{
A 3-row matrix with rows for contrast 1, contrast 2, and their difference.
Each row contains \code{Estimate}, \code{SE}, \code{df}, \code{LL}, and \code{UL},
as returned by \code{ci.lc.mean.ws}. The difference row is computed from the
contrast \code{q2 - q1}.
}
\description{
Returns confidence intervals for two linear contrasts of within-subjects
(repeated measures) group means and the difference between those contrasts.
}
\examples{
R <- matrix(c(1, .7, .7, .7, 1, .7, .7, .7, 1), 3, 3)
ci.lc.mean.ws.complex(
  alpha = .05,
  m = c(8, 11, 12),
  s = c(1.414, 2.211, 2.449),
  R = R,
  n = 10,
  q1 = c(1/3, 1/3, 1/3),
  q2 = c(1, 0, 0),
  labels = c("GrandMean", "L1Only")
)
}
