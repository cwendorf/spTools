% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spToolsInput.R
\name{sp.describe}
\alias{sp.describe}
\title{Compute Descriptive Statistics for Numeric Variables}
\usage{
sp.describe(data, vars = NULL, formula = NULL)
}
\arguments{
\item{data}{A data frame containing the variables of interest.}

\item{vars}{Optional character vector specifying the names of numeric variables to include.
If \code{NULL}, all numeric variables in \code{data} are used.}

\item{formula}{Optional formula specifying a grouping variable (e.g., \code{~ group}).
If provided, statistics are computed separately for each group.}
}
\value{
If \code{formula} is \code{NULL}, returns a matrix of descriptive statistics (N, mean, and SD)
for each variable. If \code{formula} is provided, returns a named list of such matrices, one for each group.
}
\description{
Calculates basic descriptive statistics (N, mean, and standard deviation) for numeric variables
in a data frame, optionally grouped by a factor using a formula interface.
}
\examples{
data(iris)

# Descriptive statistics for all numeric variables
describe(iris)

# Descriptive statistics for specific variables
sp.describe(iris, vars = c("Sepal.Length", "Petal.Length"))

# Grouped descriptive statistics by Species
sp.describe(iris, formula = ~ Species)

}
