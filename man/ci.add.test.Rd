% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spToolsReformat.R
\name{ci.add.test}
\alias{ci.add.test}
\title{Add Hypothesis Test Statistics to Confidence Interval Output}
\usage{
ci.add.test(results, df = NULL, n = NULL, null_value = 0, conf_level = 0.95)
}
\arguments{
\item{results}{1-row matrix output from Bonett CI functions (with Estimate, SE, LL, UL)}

\item{df}{Optional degrees of freedom (if NULL, try to infer from n)}

\item{n}{Optional sample size to infer df if df is missing}

\item{null_value}{Hypothesized null value for test statistic (default 0)}

\item{conf_level}{Confidence level used to infer df from the CI width when df is not supplied}
}
\value{
1-row matrix with columns:
Estimate, SE, t, df, p, LL, UL
}
\examples{
results <- matrix(
	c(10.5, 1.2, 8.1, 12.9),
	nrow = 1,
	dimnames = list(NULL, c("Estimate", "SE", "LL", "UL"))
)
ci.add.test(results, n = 30)
}
\description{
Adds hypothesis test statistics to confidence interval output by appending
\code{t}, \code{df}, and \code{p} columns to results that contain \code{Estimate},
\code{SE}, \code{LL}, and \code{UL}.
Degrees of freedom can be supplied directly, inferred from \code{n}, or estimated
from confidence interval width when needed.
}