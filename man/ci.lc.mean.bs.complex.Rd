% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spToolsWrappers.R
\name{ci.lc.mean.bs.complex}
\alias{ci.lc.mean.bs.complex}
\title{Confidence Intervals for Two Between-Subjects Mean Contrasts and Their Difference}
\usage{
ci.lc.mean.bs.complex(alpha, m, sd, n, q1, q2, labels = NULL)
}
\arguments{
\item{alpha}{Numeric scalar. Significance level (e.g., 0.05 for 95\% CI).}

\item{m}{Numeric vector of length J. Means for each group.}

\item{sd}{Numeric vector of length J. Standard deviations for each group.}

\item{n}{Numeric vector of length J. Sample sizes for each group.}

\item{q1}{Numeric vector of length J. Contrast weights for the first contrast.}

\item{q2}{Numeric vector of length J. Contrast weights for the second contrast.}

\item{labels}{Character vector of length 2. Optional row labels for the two contrasts.}
}
\value{
A 3-row matrix with rows for contrast 1, contrast 2, and their difference.
Each row contains \code{Estimate}, \code{SE}, \code{df}, \code{LL}, and \code{UL}.
The contrast rows use the equal-variances-assumed row from
\code{statpsych::ci.lc.mean.bs}. The difference row is computed from the
contrast \code{q2 - q1}.
}
\description{
Returns confidence intervals for two linear contrasts of independent group means
and the difference between those contrasts.
}
\examples{
ci.lc.mean.bs.complex(
  alpha = .05,
  m = c(8, 11, 12),
  sd = c(1.414, 2.211, 2.449),
  n = c(10, 10, 10),
  q1 = c(1/3, 1/3, 1/3),
  q2 = c(1, 0, 0),
  labels = c("GrandMean", "L1Only")
)
}
