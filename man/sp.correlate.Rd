% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spToolsInput.R
\name{sp.correlate}
\alias{sp.correlate}
\title{Compute Correlation or Covariance Matrices}
\usage{
sp.correlate(
  data,
  vars = NULL,
  formula = NULL,
  type = "cor",
  method = "pearson"
)
}
\arguments{
\item{data}{A data frame containing the variables of interest.}

\item{vars}{Optional character vector specifying the names of numeric variables to include.
If \code{NULL}, all numeric variables in \code{data} are used.}

\item{formula}{Optional formula specifying a grouping variable, e.g., \code{~ group}.
If provided, the correlation or covariance matrix is computed separately for each group.}

\item{type}{Character string indicating whether to compute \code{"cor"} (correlation) or \code{"cov"} (covariance).
Default is \code{"cor"}.}

\item{method}{Character string specifying the correlation method: one of \code{"pearson"} (default), \code{"spearman"}, or \code{"kendall"}.
Ignored if \code{type = "cov"}.}
}
\value{
If \code{formula} is \code{NULL}, returns a correlation or covariance matrix for the selected variables.
If \code{formula} is provided, returns a named list of matrices, one for each group level.
}
\description{
Computes a correlation or covariance matrix for selected numeric variables in a data frame,
optionally grouped by a factor using a formula interface.
}
\examples{
data(iris)

# Correlation matrix for all numeric variables
sp.correlate(iris)

# Covariance matrix for specific variables
sp.correlate(iris, vars = c("Sepal.Length", "Petal.Length"), type = "cov")

# Grouped correlation matrices by Species
sp.correlate(iris, formula = ~ Species)

}
