% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spToolsWrappers.R
\name{ci.prop.compare}
\alias{ci.prop.compare}
\title{Confidence Intervals to Compare Two Proportions}
\usage{
ci.prop.compare(alpha, f, n, type = c("difference", "ratio", "both"))
}
\arguments{
\item{alpha}{Numeric scalar. Significance level (e.g., 0.05 for 95\% CI).}

\item{f}{Numeric vector of length 2. Frequencies for groups 1 and 2.}

\item{n}{Numeric vector of length 2. Sample sizes for groups 1 and 2.}

\item{type}{Character scalar indicating comparison rows to include:
\code{"difference"}, \code{"ratio"}, or \code{"both"}.}
}
\value{
A matrix with rows for group 1, group 2, and one or two comparison
rows depending on \code{type}. Columns are \code{Estimate}, \code{SE}, \code{LL}, and \code{UL}.
}
\description{
Returns confidence intervals for each group proportion and comparison rows
for the proportion difference, the proportion ratio, or both.
}
\details{
The difference row is obtained from \code{ci.prop2.vec}. The ratio row is
obtained from \code{statpsych::ci.ratio.prop2}. Because \code{ci.ratio.prop2} does
not return a standard error column, \code{SE} is set to \code{NA} for the ratio row.
}
\examples{
ci.prop.compare(
  alpha = .05,
  f = c(57, 15),
  n = c(100, 100),
  type = "both"
)

}
